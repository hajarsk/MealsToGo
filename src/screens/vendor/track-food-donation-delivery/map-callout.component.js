import React from "react";

import { CompactCheckpointInfo } from "../vendor-homescreen/compact-info.component";

export const MapCallout = ({ checkpoint }) => (
    <CompactCheckpointInfo checkpoint={checkpoint} />
);